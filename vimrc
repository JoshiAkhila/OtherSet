" Show file encoding
set encoding=utf-8

" Show File number
set number

" Try to determine the type of a file based on its name and contents. 
filetype indent plugin on

" Enable Syntax highlighting
syntax on

" Set Auto Indentation
set autoindent

" Instead of failing a command because of unsaved changes, raise a dialogue 
" asking if you wish to save changed files.
set confirm

" Use visual bell instead of beeling
set visualbell

" Indentation settings for using 4 spaces instead of tabs.
set shiftwidth=4
set softtabstop=4
set expandtab

" Show file stats
set ruler

" Always show current mode
set showmode

" Set ColorScheme
colorscheme koehler
